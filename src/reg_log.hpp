#include <iostream> //Libary for inputing and outputing.
#include <fstream>  //Including fstream library for doing functions with folders.

using namespace std;
class Registry {		//Creating class.
          public:
                 string login;
                 string registers;
                 string l,pass,pass2;
 
};
void Case1(Registry);
void Case2(Registry); 
